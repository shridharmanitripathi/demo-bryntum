{
  "dependencies": [
    {
      "src": "../_shared",
      "out": "../_shared/dist"
    }
  ],
  "title": "Angular Routing + NgRx demo",
  "description": "This example shows how to use Bryntum Scheduler in an Angular application with routing and with state managed by NgRx."
}
